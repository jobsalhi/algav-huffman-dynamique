"""Encodeur (Ayoub)."""

class Encoder:  # (Ayoub)
    """encode_symbol à implémenter."""
    def __init__(self, tree):
        self.tree = tree

    def encode_symbol(self, symbol):
        """Retourne bits du symbole (logique à faire)."""
        pass
