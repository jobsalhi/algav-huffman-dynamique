# (Ayoub) Implémentera la logique d'encodage symbole par symbole.

class Encoder:
    """Encodeur Huffman dynamique.
    Méthode principale: encode_symbol(symbol)
    - symbole connu -> retourne son code actuel
    - nouveau symbole -> code NYT + UTF-8(symbol)
    - déclenche mise à jour de l'arbre
    """
    # (Ayoub) à implémenter
    def __init__(self, tree):
        self.tree = tree

    def encode_symbol(self, symbol):
        """Retourne la séquence de bits correspondant au symbole.
        (Ayoub) implémentera la logique complète.
        """
        pass
